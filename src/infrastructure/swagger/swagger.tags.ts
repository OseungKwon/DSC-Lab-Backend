import { SwaggerTag } from '@infrastructure/types/type';

export const SwaggerTags: SwaggerTag = {
  member: 'Member related API',
  board: 'Board related API',
  comment: 'Comment related API',
  category: 'Category related API',
  manager: 'Manager related API',
};
