export enum Role {
  GRADUATE_STUDENT = 'graduate-student',
  POST_GRADUATE_STUDENT = 'post-graduate-student',
  PROFESSOR = 'professor',
}

export enum Approved {
  PENDING = 'pending',
  APPROVE = 'approve',
  REJECT = 'reject',
  RESTRICT = 'restrict',
}
